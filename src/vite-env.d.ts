/// <reference types="vite/client" />

// The standard Vite shim. Declared here rather than via tsconfig `types`,
// because setting `types` replaces the default @types/* auto-include and would
// silently drop ambient packages this project relies on.
